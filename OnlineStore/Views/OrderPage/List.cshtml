@model IEnumerable<OnlineStore.Models.OrderDto>

<h2>All Orders</h2>
@foreach (OnlineStore.Models.OrderDto orderDto in Model)
{
    <div>
        <a href="/OrderPage/Show/@orderDto.OrderId">@orderDto.OrderId @orderDto.OrderDate @orderDto.CustomerName</a>
    </div>

}